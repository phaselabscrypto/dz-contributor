# ── Supply-chain hardening ─────────────────────────────────────────
# Always save exact versions (no ^ or ~ prefix).
save-exact=true

# Required while @solana/web3.js v1 has unresolvable peer-dep trees.
legacy-peer-deps=true
